NEXT_PUBLIC_API_URL=http://localhost:3001/api
# Optional: when the Next.js server runs inside Docker, set this so server-side fetches use the API
# container (e.g. http://api:3001/api). Omit for local `pnpm dev` on the host.
# API_INTERNAL_URL=http://api:3001/api
# Optional display name in dashboard chrome
NEXT_PUBLIC_SCHOOL_NAME=Learning Analytics
